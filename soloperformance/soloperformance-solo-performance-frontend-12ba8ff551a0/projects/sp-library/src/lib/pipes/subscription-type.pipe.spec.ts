import { SubscriptionTypePipe } from './subscription-type.pipe';

describe('SubscriptionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
