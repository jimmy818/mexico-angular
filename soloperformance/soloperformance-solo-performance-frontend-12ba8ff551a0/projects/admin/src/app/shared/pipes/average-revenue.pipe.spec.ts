import { AverageRevenuePipe } from './average-revenue.pipe';

describe('AverageRevenuePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageRevenuePipe();
    expect(pipe).toBeTruthy();
  });
});
