import { AdminChartSubtitlePipe } from './admin-chart-subtitle.pipe';

describe('AdminChartSubtitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminChartSubtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
