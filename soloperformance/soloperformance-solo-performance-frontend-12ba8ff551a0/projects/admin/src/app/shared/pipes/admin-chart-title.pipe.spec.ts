import { AdminChartTitlePipe } from './admin-chart-title.pipe';

describe('AdminChartTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminChartTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
