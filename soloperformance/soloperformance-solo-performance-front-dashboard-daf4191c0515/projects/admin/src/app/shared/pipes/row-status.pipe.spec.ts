import { RowStatusPipe } from './row-status.pipe';

describe('RowStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RowStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
